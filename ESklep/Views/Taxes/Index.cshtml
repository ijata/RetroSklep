@model IEnumerable<ESklep.Models.Tax>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tax_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tax_value)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tax_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tax_value)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.tax_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.tax_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.tax_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
